{
  "name": "node-api",
  "version": "1.0.0",
  "description": "",
  "engines": {
    "node": ">=20.0.0"
  },
  "type": "commonjs",
  "main": "src/main.js",
  "scripts": {
    "start": "node --env-file=.env src/main.js",
    "dev": "nodemon",
    "test": "node --test",
    "test:coverage": "npm test -- --experimental-test-coverage",
    "test:report": "npm test:coverage -- --test-reporter=lcov --test-reporter-destination=lcov.info",
    "lint": "standard --fix",
    "prepare": "husky",
    "pretest:report": "./scripts/require-node21.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "git-commit-msg-linter": "^5.0.6",
    "husky": "^9.0.10",
    "nodemon": "^3.0.3",
    "standard": "^17.1.0"
  },
  "standard": {
    "env": [
      "node"
    ]
  },
  "dependencies": {
    "@fastify/cors": "^9.0.1",
    "@fastify/swagger": "^8.14.0",
    "@fastify/swagger-ui": "^2.1.0",
    "fastify": "^4.26.0"
  }
}
