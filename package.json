{
  "name": "api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "node --test --experimental-test-coverage",
    "test:coverage": "node --test --experimental-test-coverage --test-reporter=lcov --test-reporter-destination=lcov.info",
    "lint": "standard --fix",
    "prepare": "husky",
    "pretest:coverage": "./scripts/require-node21.sh"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "git-commit-msg-linter": "^5.0.6",
    "husky": "^9.0.10",
    "standard": "^17.1.0"
  },
  "standard": {
    "env": [
      "node"
    ]
  }
}
